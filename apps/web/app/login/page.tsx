import LoginClient from '@/components/auth/LoginClient'

export const metadata = {
  title: 'PSGMX | Sign In',
  description: 'Sign in to your PSGMX department ecosystem account',
}

export default function LoginPage() {
  return <LoginClient />
}
